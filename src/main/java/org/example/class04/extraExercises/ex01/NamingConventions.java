package org.example.class04.extraExercises.ex01;
//❖ isValidJavaIdentifier(String): recebe o nome do identificador e verifica se ele está de acordo com as
//regras nome de identificadores em Java.

//Classes
//Class names should be nouns, in mixed case with the first letter of each internal word capitalized.
//Try to keep your class names simple and descriptive. Use whole words-avoid acronyms and abbreviations
//(unless the abbreviation is much more widely used than the long form, such as URL or HTML).
//
//Methods
//Methods should be verbs, in mixed case with the first letter lowercase, with the first letter of each internal word capitalized
//
//Variables	Except for variables, all instance, class, and class constants are in mixed case with a lowercase first letter.
//Internal words start with capital letters. Variable names should not start with underscore _ or dollar sign $ characters, even though both are allowed.
//Variable names should be short yet meaningful.The choice of a variable name should be mnemonic- that is, designed to indicate to
//the casual observer the intent of its use. One-character variable names should be avoided except for temporary "throwaway" variables.
//Common names for temporary variables are i, j, k, m, and n for integers; c, d, and e for characters.
//
//Constants	The names of variables declared class constants and of ANSI constants should be all uppercase with words
//separated by underscores ("_"). (ANSI constants should be avoided, for ease of debugging.)

public class NamingConventions {

    private NamingConventions() {
    }

    static boolean isFollowingConvention(String string, Convention convention){

    }

    static String fromConstToVariable(String string){

    }

    static String fromVariableToConst(String string){

    }

    static boolean isValidJavaIdentifier(String string){

    }
}
